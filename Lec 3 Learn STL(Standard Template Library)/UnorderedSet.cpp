#include <bits/stdc++.h>
using namespace std;

void explainUnorderedSet()
{ 
    unordered_set<int>st;
    //Same Function As Set
    //Implemeneted using Hash Table
    //O(1)Best, Average Operation O(n) Worst(Very Less Possible)
    //Not Sorted Random Order unlike set
    //lower_bound and upper_bound does not work like set

}

int main()
{
    explainUnorderedSet();
    return 0;
}
