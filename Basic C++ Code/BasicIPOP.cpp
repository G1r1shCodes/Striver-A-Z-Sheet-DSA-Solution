#include<iostream> //Used to import input output(cin,cout) functions
#include<bits/stdc++.h> //used for all standard library functions.
using namespace std; //used for cin and cout no need to write std::cin or std::cout
int main()
{
    int x; //declaring a variable named as x of data type int.
    cout<<"Enter Number:";
    cin>>x; //taking input from user and storing in x.
    cout<<"Number is:"<<x<<endl;//printing the number stored in x.

    return 0; //returning the value of function main() which is int type so it will be returning an integer value.
}